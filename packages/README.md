# Packages

This directory contains shared libraries and domain modules used by multiple apps.

Planned MVP packages:
- `protocol/` - shared message schemas and core types
- `game-engine/` - checkers rules and game state transitions
- `board-model/` - board coordinates, calibration, and square/world mapping
